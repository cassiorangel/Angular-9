import { LimiterCaracterPipe } from './limiter-caracter.pipe';

describe('LimiterCaracterPipe', () => {
  it('create an instance', () => {
    const pipe = new LimiterCaracterPipe();
    expect(pipe).toBeTruthy();
  });
});
